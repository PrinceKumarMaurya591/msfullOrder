name: Check-POM-Path

on:
  push:
    branches:
      - main

jobs:
  check_path:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: List files in working directory
      run: ls -R


